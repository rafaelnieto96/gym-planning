This project acts as a front-end for its Spring Boot counterpart (https://github.com/rafaelnieto96/gym). It's about a calendar where the user can add the exercises they will do each day of the week by clicking on the empty spaces and send data through CRUD operations to the microservice.
